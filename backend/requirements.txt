# Core Framework
fastapi==0.111.0
uvicorn==0.30.1
jinja2>=3.1.0
boto3>=1.34.0

# Database
sqlalchemy==2.0.36
greenlet>=3.0.0
psycopg[binary]==3.3.3
alembic>=1.13.0

# Security & Auth
itsdangerous==2.2.0

# Configuration
python-dotenv==1.0.1
pydantic-settings==2.2.1

# HTTP Client & File Handling
httpx>=0.28.1
python-multipart==0.0.19
requests>=2.31  # used by src/services/stt_service.py for Sarvam REST calls
sarvamai>=0.1   # Sarvam batch STT for clips > 30 s (used by stt_service.py)
mutagen>=1.47   # audio duration probe for routing REST vs Batch

# AI & SMS Services
google-genai>=2.8.0
twilio>=9.0.0

# Rate Limiting
slowapi>=0.1.9

# Error monitoring (optional — only active when SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0

# Testing
pytest==8.2.1
pytest-asyncio>=0.23

# Dev-only: standalone tester UI for the summarisation module.
# Note: Streamlit upgrades starlette past FastAPI 0.111's pin — install in a
# separate venv if you want to run the full FastAPI app in the same env.
streamlit>=1.40
Pillow>=10.0